import { Command } from '@alipay/faas-db-builder';
import type { CloudConfig } from '@alipay/faas-server-utils';
import { CollectionDescription, DeleteResult } from '../types';
import { Collection } from './Collection';
export declare class Database {
    #private;
    command: Command;
    constructor(config: CloudConfig);
    collection(collectionName: string): Collection;
    createCollection(collectionName: string): Promise<CollectionDescription>;
    getCollection(collectionName: string): Promise<CollectionDescription | null>;
    listCollection(limit?: number, skip?: number): Promise<Array<CollectionDescription>>;
    deleteCollection(collectionName: string): Promise<DeleteResult>;
}
