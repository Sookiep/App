import type { HttpClientResponse } from 'urllib';
import type { CloudConfig } from '@alipay/faas-server-utils';
import type { HttpRequest, HttpRequestResult } from '../../types';
export declare class HttpRequestContext<T, R> {
    readonly req: HttpRequest;
    readonly config: CloudConfig;
    res: HttpClientResponse<T>;
    result?: HttpRequestResult<R>;
    constructor(req: HttpRequest, config: CloudConfig);
    get requestId(): string;
}
